import { LearningOptionIconPipe } from './learning-option-icon.pipe';

describe('LearningOptionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningOptionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
